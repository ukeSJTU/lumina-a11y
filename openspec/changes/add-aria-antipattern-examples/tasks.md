## 1. Implementation
- [ ] 1.1 Create `examples/icon-only-buttons/` with self-contained `index.html` + `style.css` demonstrating icon-only buttons without accessible names.
- [ ] 1.2 Create `examples/unlabeled-form/` with self-contained `index.html` + `style.css` demonstrating form fields without associated labels.
- [ ] 1.3 Create `examples/custom-controls/` with self-contained `index.html` + `style.css` demonstrating custom controls without semantic roles or keyboard support.
- [ ] 1.4 Update `justfile` with a `server` recipe to serve `examples/<name>` on port 5500.
- [ ] 1.5 Validation: run `just server icon-only-buttons` and open `http://localhost:5500`, repeat for the other examples.
