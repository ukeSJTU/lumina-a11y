## 1. Implementation
- [x] 1.1 Create `examples/icon-only-buttons/` with self-contained `index.html` + `style.css` demonstrating icon-only buttons without accessible names.
- [x] 1.2 Create `examples/unlabeled-form/` with self-contained `index.html` + `style.css` demonstrating form fields without associated labels.
- [x] 1.3 Create `examples/custom-controls/` with self-contained `index.html` + `style.css` demonstrating custom controls without semantic roles or keyboard support.
- [x] 1.4 Update `justfile` with a `server` recipe to serve `examples/<name>` on port 5500.
- [x] 1.5 Update README to list the new example pages and server command.
- [x] 1.6 Update `openspec/project.md` to require documentation updates with major changes.
- [x] 1.7 Validation: run `just server icon-only-buttons` and open `http://localhost:5500`, repeat for the other examples.
